{
  "name": "loan-tracker",
  "version": "1.5.3",
  "description": "Zero-install browser-based loan tracker",
  "scripts": {
    "build": "node build.js",
    "test": "node --test test/regression.js",
    "test:update": "node test/regression.js --update-snapshots"
  }
}
